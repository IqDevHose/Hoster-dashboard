module.exports = {
    darkMode: ["class"],
    content: [
    "./src/**/*.{html,js,jsx,ts,tsx}", // Make sure this includes your JSX files
  ],
  theme: {
  	extend: {
  		borderRadius: {
  			lg: 'var(--radius)',
  			md: 'calc(var(--radius) - 2px)',
  			sm: 'calc(var(--radius) - 4px)'
  		},
  		colors: {}
  	}
  },
  plugins: [require("tailwindcss-animate")],
};
